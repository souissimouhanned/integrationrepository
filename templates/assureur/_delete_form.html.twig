<form method="post" action="{{ path('assureur_delete', {'id': assureur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assureur.id) }}">
    <button class="btn">Delete</button>
</form>
