<form method="post" action="{{ path('detail_voiture_delete', {'id': detail_voiture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_voiture.id) }}">
    <button class="btn">Delete</button>
</form>
